version: "3.8"

services:
  frontend:
    build: .
    container_name: frontend_app
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    command: sh -c "npm run build && npm run start"